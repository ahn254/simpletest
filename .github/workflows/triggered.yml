run-name: triggered one
on:
  workflow_dispatch:
    inputs:
      IMAGE_NAME:
        description: 'image Name'
        required: false

jobs:
  trigger-test:
    runs-on: ubuntu-latest
    name: trigger test
    steps:
      - name: trigger 1  
        shell: pwsh  
        run: |
          write-host "it works - ${{ inputs.IMAGE_NAME }}"
